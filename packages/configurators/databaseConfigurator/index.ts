import { Model } from "@models/Model";
import { AppConfiguratorConfig } from "..";

export async function DatabaseConfigurator(
  models: Model[],
  config: AppConfiguratorConfig["database"]
) {
  // models.map()
}
